# --- Demo mode -------------------------------------------------------------
# When true (or when ANTHROPIC_API_KEY is missing), the app runs entirely on
# canned demo data: no AI, Supabase, or Stripe calls are made. This lets you
# test and demo the full product with zero configuration.
DEMO_MODE=true

# --- Site --------------------------------------------------------------------
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- AI (Anthropic) ----------------------------------------------------------
# Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5

# --- Supabase ------------------------------------------------------------------
# Project settings > API. The service role key is server-only, never expose it
# to the client.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# --- Stripe --------------------------------------------------------------------
# Two offers: a one-time 4,90€ analysis unlock (mode=payment) and a 6,90€/month
# GuestMirror Plus subscription (mode=subscription). Create both Products/Prices
# in the Stripe dashboard (or via the API) and paste their price IDs below.
STRIPE_SECRET_KEY=
STRIPE_PRICE_ONE_TIME=
STRIPE_PRICE_PLUS_MONTHLY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# --- Email (optional) ---------------------------------------------------------
# Sends the unlocked-report link by email right after a real payment is
# confirmed (Stripe webhook), so the customer isn't only relying on keeping
# a browser tab open. Without RESEND_API_KEY, this silently no-ops -- the
# unlock itself is unaffected either way. Get a key at https://resend.com,
# and verify your sending domain there before setting RESEND_FROM_EMAIL to
# an address on it (e.g. "GuestMirror <hello@guestmirror.fr>"); until the
# domain is verified, Resend can only deliver to the account's own email.
RESEND_API_KEY=
RESEND_FROM_EMAIL=

# --- Analytics (optional) -----------------------------------------------------
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=

# --- Admin dashboard -----------------------------------------------------------
# Protects /admin/analytics with HTTP Basic Auth. Required to view the
# dashboard; the funnel still tracks fine without it, you just can't see it.
ADMIN_USERNAME=
ADMIN_PASSWORD=
