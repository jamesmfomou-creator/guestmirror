/**
 * Single source of truth for the product name. Change it here and it
 * propagates everywhere (UI copy, metadata, AI prompt, share cards). No
 * component or page should hardcode the name directly.
 */
export const BRAND_NAME = "GuestMirror";

/** Lowercase, no-space form used in filenames (downloads) and the domain. */
export const BRAND_SLUG = "guestmirror";

export const BRAND_DOMAIN = "guestmirror.app";

export const BRAND_TAGLINE = "Vois ton annonce avec les yeux d'un voyageur.";
